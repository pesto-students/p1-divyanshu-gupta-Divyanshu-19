const mathOperations = {
    sum(a,b){
        return a+b;
    },
    diff(a,b) {
        return a-b;
    },
    product(a,b){
        return a*b;
    }
}

module.exports = mathOperations;

// A object that contains function for addition, subtraction and product is being exported